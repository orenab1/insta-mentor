export interface User {
    username: string;
    token: string;
    photoUrl: string;
    id: number;
}

export interface UserSummary {
    id:number;
    username: string;
    title: string;
    photoUrl: string;
}